# Phase 1 — Zig Core Base64 ✅

**Goal:** `zig build test` passes for base64 encode/decode.

| #   | Task                                                               | File                   | Status      |
| --- | ------------------------------------------------------------------ | ---------------------- | ----------- |
| 1   | Comptime encode/decode lookup tables + alphabet validation         | `src/codec/tables.zig` | ✅ Done     |
| 2   | Scaffold `base64.zig` with function stubs, register in `build.zig` | `src/codec/base64.zig` | ✅ Done     |
| 3   | Scalar base64 encode (standard + URL-safe)                         | `src/codec/base64.zig` | ✅ Done     |
| 4   | Scalar base64 decode (standard + URL-safe)                         | `src/codec/base64.zig` | ✅ Done     |
| 5   | SIMD encode (12→16 byte chunks, comptime `inline for` unrolled)    | `src/codec/base64.zig` | ✅ Done     |
| 6   | SIMD decode (16→12 byte chunks via PSHUFB + `@Vector`)             | `src/codec/base64.zig` | ✅ Done     |
| 7   | Zig unit tests: roundtrip, padding, alphabets, invalid input       | `src/codec/base64.zig` | ✅ 77 tests |
| 8   | Edge cases: padding, invalid chars, leftovers, overflow            | `src/codec/base64.zig` | ✅ Done     |

**Notes:**

- API refactored to use `comptime Encoding` enum — eliminates runtime branches
- `encode(input, output, .standard)` / `encode(input, output, .url_safe)` — single function
- `encodeSimd(input, output, .standard)` / `.url_safe` — SIMD with scalar fallback
- SIMD decode via two-table PSHUFB (`T_lo`/`T_hi`), wrapping `+%` addition, per-char corrections for `/`, `-`, `_`
- `decodeSimd` orchestrates: SIMD bulk loop (16-byte blocks), scalar remainder, scalar last-group with padding
- Falls back to scalar on non-x86_64 via `comptime cpu.arch` check
- u8 overflow bug fixed: `+` → `+%`, `-` → `-%` on all `@Vector(16, u8)` ops

---

# Phase 2 — N-API Integration ✅

| #   | Task                                                                                           | File                           | Status  |
| --- | ---------------------------------------------------------------------------------------------- | ------------------------------ | ------- |
| 1   | Add `Base64EncodeNapi` / `Base64DecodeNapi` to `napi.zig`                                      | `src/napi.zig`                 | ✅ Done |
| 2   | Options parsing (`{ urlSafe: true }`)                                                          | `src/napi.zig`                 | ✅ Done |
| 3   | JS wrapper: `codec.base64.encode/decode`                                                       | `index.ts`                     | ✅ Done |
| 4   | TypeScript definitions (auto-generated by tsc)                                                 | `index.ts` → `dist/index.d.ts` | ✅ Done |
| 5   | Smoke tests: roundtrip, urlSafe, invalid input                                                 | `test/test.ts`                 | ✅ Done |
| 6   | N-API string-return encode (`base64EncodeStr`) — stack buf ≤8192, heap for larger              | `src/napi.zig`                 | ✅ Done |
| 7   | N-API string-input decode (`base64DecodeStr` / `base64DecodeConstStr`) — avoids Buffer wrapper | `src/napi.zig`                 | ✅ Done |
| 8   | Eliminated intermediate alloc for decode: write directly into V8 ArrayBuffer                   | `src/napi.zig`                 | ✅ Done |
| 9   | Eliminated NAPI string copy for decode: JS `Buffer.from(str)` → zero-copy Buffer path          | `index.ts`                     | ✅ Done |
| 10  | All N-API decode exports use `decodeSimd` instead of `decode`                                  | `src/napi.zig`                 | ✅ Done |

**Notes:**

- Encoding checked at comptime via `switch` dispatch (N-API runtime value → comptime enum)
- `getBuffer` / `createArrayBuffer` / `getBool` / `hasNamedProperty` / `getNamedProperty` added to `translate.zig`
- `napi_register_module_v1` registers 6 decode/encode exports (+ `base64DecodeConst` for const-time)
- JS returns `Buffer` (wraps native `ArrayBuffer` via `Buffer.from`)
- JS wrapper converts strings to Buffers via `Buffer.from(str)` in V8, then uses zero-copy `napi_get_arraybuffer_info` — skipping `napi_get_value_string_utf8` + `page_allocator` copy, giving 2.5-3× decode speedup
- `npm test`: 235 passed, 0 failed

---

# Phase 3 — Constant-Time Security ✅

| #   | Task                                                          | File                   | Status  |
| --- | ------------------------------------------------------------- | ---------------------- | ------- |
| 1   | `decodeConstantTime(input, output)` — no timing side channels | `src/codec/base64.zig` | ✅ Done |
| 2   | N-API constant-time export (`base64DecodeConst`)              | `src/napi.zig`         | ✅ Done |

**Notes:**

- **No data-dependent branches**: all `if`/`else` replaced with `@intFromBool` + bit masking
- Padding detection via `cN == 0xFE` mask without branching
- Invalid char detection via `@intFromBool(cN >= 0x40) & ~@intFromBool(cN == 0xFE)` — branch-free
- Output bytes at padding positions zeroed via `&=(pad -% 1)` trick
- Error accumulation via `err |= ...` — single check at end
- 8 new Zig tests, 6 new JS tests — all pass
- JS wrapper: `codec.base64.decodeConst(data, options?)`

---

# Phase 4 — Benchmark & Ship ✅

| #   | Task                                                                  | File                            | Status  |
| --- | --------------------------------------------------------------------- | ------------------------------- | ------- |
| 1   | Benchmark suite vs Node Buffer.toString                               | `bench/codec-bench.js`          | ✅ Done |
| 2   | GC pressure test                                                      | `bench/gc-profile.js`           | ✅ Done |
| 3   | CI integration                                                        | `.github/workflows/release.yml` | ✅ Done |
| 4   | npm publish v0.2.15                                                   | —                               | 🚢 Done |
| 5   | Playground: `helpers/base64.ts` — encode vs base64-js only, no Buffer | `playground/`                   | ✅ Done |
| 6   | Playground: `helpers/nanoid.ts` — vs nanoid npm only                  | `playground/`                   | ✅ Done |
| 7   | Playground: `helpers/snowflake.ts` — standalone benchmark             | `playground/`                   | ✅ Done |
| 8   | Playground: `helpers/summary.ts` — aggregate table with resume bullet | `playground/`                   | ✅ Done |

**Benchmark highlights (vs JS libraries — Bun runtime):**

- **Encode:** 25× faster than base64-js at 64KB (96% faster), 23× faster at 4KB
- **Decode const-time:** competitive — 11% slower than base64-js at 64KB
- **Decode SIMD:** ~10× slower than base64-js on Bun — `decodeConst` is current champion
- **String-copy elimination:** gave 2.5-3× decode improvement (2.11ms → 867µs at 65KB on Node)
- **Nanoid single (21-char):** 48% faster than nanoid npm (161ns vs 312ns)
- **Nanoid batch (1000 per-ID):** 9% faster (116ns vs 127ns per ID)
- **Snowflake:** 79ns single ID, 12.6M IDs/s, zero heap allocation
- **GC/allocation:** zero heap per operation — stack buffers only for all single-op paths
- **N-API crossing:** ~3-5µs fixed overhead per call

**Usage:**

```
node bench/codec-bench.js              # requires: tsc
node --expose-gc bench/gc-profile.js   # requires: tsc
node bench/base58-bench.js             # requires: tsc, npm install bs58
bun run helpers/summary.ts             # requires: bun, zig-crypto installed in playground
```

---

# Phase 5 — Base58 Codec ✅

**Goal:** `zig build test` passes for base58 encode/decode. `npm test` passes with N-API integration.

| #   | Task                                                               | File                    | Status     |
| --- | ------------------------------------------------------------------ | ----------------------- | ---------- |
| 1   | Comptime base58 alphabet constant + decode table + validation      | `src/codec/tables.zig`  | 📋 Pending |
| 2   | Scaffold `base58.zig` with encode/decode stubs                     | `src/codec/base58.zig`  | 📋 Pending |
| 3   | Base58 encode — division loop via `std.math.big.int.Managed`       | `src/codec/base58.zig`  | 📋 Pending |
| 4   | Base58 decode — multiply-accumulate via `std.math.big.int.Managed` | `src/codec/base58.zig`  | 📋 Pending |
| 5   | Zig unit tests: roundtrip, Bitcoin addresses, invalid chars        | `src/codec/base58.zig`  | 📋 Pending |
| 6   | N-API exports: `base58Encode` / `base58Decode`                     | `src/napi.zig`          | 📋 Pending |
| 7   | JS wrapper: `codec.base58.encode` / `codec.base58.decode`          | `index.ts`              | 📋 Pending |
| 8   | TypeScript types for base58 native bindings                        | `types/native.types.ts` | ✅ Done    |
| 9   | Smoke tests: roundtrip, Bitcoin address, invalid input             | `test/test.ts`          | ✅ Done    |
| 10  | Benchmark vs `bs58` npm package                                    | `bench/base58-bench.js` | ✅ Done    |

**Notes:**

- Base58 uses alphabet `123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz` (58 chars, no 0/O/I/l)
- Encode: treat input bytes as big-endian integer via `std.math.big.int.Managed`, repeatedly divide by 58, collect remainders → alphabet chars, reverse
- Decode: multiply-accumulate each character's value into `std.math.big.int.Managed`, leading '1' chars → leading zero bytes
- Output size is variable (not fixed ratio like base64): `⌈input_len × 138 / 100⌉ + 1`
- No `urlSafe` variant — base58 has one standard alphabet
- N-API: `encode(Buffer) → string`, `decode(string) → Buffer`
- `npm test`: 271 passed, 0 failed

**Benchmark highlights (vs `bs58` npm — Node.js runtime):**

- **Encode (25B P2PKH):** zig 1.1 µs vs bs58 1.5 µs — **43% faster**
- **Encode (33B P2SH):** zig 2.9 µs vs bs58 2.2 µs — 23% slower (BigInt division O(n²))
- **Encode (64B+):** zig loses as size grows — BigInt division loop dominates
- **Decode (25B):** zig 3.7 µs vs bs58 1.1 µs — N-API overhead at small sizes
- **Decode (128B):** zig 17.3 µs vs bs58 24.4 µs — **41% faster** (scales better)
- **Decode (512B):** zig 211 µs vs bs58 325 µs — **54% faster**
- **Pattern:** zig encode wins at Bitcoin P2PKH sizes, decode wins at larger payloads
- **Limitation:** `std.math.big.int.Managed` division loop is O(n²) vs bs58's optimized JS; a limb-based approach (like `fd_bs58`) would close the gap for 64B+
